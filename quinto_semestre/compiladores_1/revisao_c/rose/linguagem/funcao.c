// estudar com calma 

#include <stdio.h>

int main(void)
{
    int d;
    while (scanf("%d", &d) != EOF)
    {
        printf("%d\n", d);
    }
    // ctrl + d = end of file
    // while ( scanf ( " % d " , & d ) == 1) { }

    return 0;
}
